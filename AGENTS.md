# AI Start Here

Maintainers: Claude Code, Codex, Gemini.

Read order:
1. `PROJECT_CONTEXT.md`
2. `CLAUDE.md` / `CODEX.md` / `GEMINI.md` for your own tool
3. `SEOUL20266_UI_STYLE_GUIDE.md` only for UI / layout / share-mode / visual bug tasks

Rules:
- Use `PROJECT_CONTEXT.md` as the shared source of truth.
- Update `PROJECT_CONTEXT.md` after meaningful code, UI, data, cache, map, share, or docs changes.
- If UI rules changed, also update `SEOUL20266_UI_STYLE_GUIDE.md`.
- Never overwrite another editor's update-log entry; add a follow-up entry instead.
- Before writing `Updated at`, run `TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M CST'`.
- Do not auto-load `FULL_APP_REVIEW_BUNDLE.md`, `COMPLETE_APP_REVIEW_BUNDLE.md`, or `MULTI_TRIP_REVIEW_BUNDLE.md` unless the task explicitly needs a full bundled review.

Review prompt:
- Use `REVIEW_PROMPT.md` when you need the full audit prompt.
