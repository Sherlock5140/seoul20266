@AGENTS.md

# Claude Code Adapter

- Project Skills use `.claude/skills` discovery shims that load the canonical `.agents/skills` workflow.
- When `AGENTS.md` requires a plan, use Claude Plan Mode.
- Keep private, machine-specific preferences in `CLAUDE.local.md`.

## Compact Instructions

Preserve the objective, status, changed files, validations run/not run, risks, blockers, remaining work, and deployment cache/query versions. If a project Skill remains active, reread its canonical `.agents/skills` workflow after compaction.
