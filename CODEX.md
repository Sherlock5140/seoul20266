# Codex Adapter

Shared governance comes from `AGENTS.md`. Current routes and versions come from `PROJECT_CONTEXT.md`.

- Use `rg` and the stable symbols in the routed guide before opening large source files.
- Use small, scoped patches and preserve unrelated worktree changes.
- Canonical project skills live under `.agents/skills/`; use the router before selecting one.
- Use a plan for cross-cutting or uncertain work, then validate only the affected behavior.
- Finish with changed files, validation evidence, checks not run, risks, external-state changes, and remaining work.
