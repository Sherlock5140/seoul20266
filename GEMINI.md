@AGENTS.md

# Gemini CLI Adapter

- Use `/memory show` to inspect active context, `/memory reload` after context-file changes, and `/skills reload` after Skill changes.
- Project Skills use Gemini's native `.agents/skills` workspace alias.
